#!/bin/bash

# Basic Openbox setup commands (optional)
# Example: Set background color
# xsetroot -solid grey

# Disable screen blanking / power saving
xset s off -dpms

# Run the main application (Firefox in kiosk mode pointing to localhost)
# We assume the Next.js app will eventually run on port 3000
(sleep 5 && firefox --kiosk http://localhost:3000) &

# Start a terminal (optional, for debugging)
# xterm &
