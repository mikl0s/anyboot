#!/bin/sh
set -e

# Enable custom first-boot service
systemctl enable anyboot-first-boot.service

# Enable standard services (if not already enabled by package install)
systemctl enable lightdm.service
systemctl enable NetworkManager.service
systemctl enable mongodb.service # Note: Config for data dir needed later
systemctl enable redis-server.service
